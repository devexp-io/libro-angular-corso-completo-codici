import { BkgDirective } from './bkg.directive';

describe('BkgDirective', () => {
  it('should create an instance', () => {
    const directive = new BkgDirective();
    expect(directive).toBeTruthy();
  });
});
