import { LogInputDirective } from './log-input.directive';

describe('LogInputDirective', () => {
  it('should create an instance', () => {
    const directive = new LogInputDirective();
    expect(directive).toBeTruthy();
  });
});
